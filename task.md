4 things:
    1. there is 3 tick counts make only the one on the top right the main one and the other two should be deleted
    2. remove the background arround label and buttons
    3. in gui mode logs should be displayed in a logs tab instead of the logs file
    4. if possible to make the console input part of the gui do it however it should look like a terminal in the console
