#ifndef PROJECT_ETHOS_EMULATOR_CORE_H
#define PROJECT_ETHOS_EMULATOR_CORE_H

#include "Emulator.h"

/* Returns a pointer to the emulator's internal simulated memory array. */
void **getMEM(Emulator *emu);

#endif